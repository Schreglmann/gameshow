#!/bin/sh
# Scoped gate — runs only the checks the staged change set can affect.
# Full-suite backstop: .github/workflows/ci.yml (CI), on every push to every branch.
# See AGENTS.md §7 (Verification).
exec node scripts/verify-changed.cjs --staged
