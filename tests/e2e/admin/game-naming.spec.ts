import { test, expect } from '@playwright/test';

// Spec: specs/game-naming.md (Planned)
test.describe('Game naming improvements', () => {
  test.fixme('rename updates file and rewrites gameOrder references', async () => {
    // TODO
  });

  test.fixme('rename to an existing filename is rejected', async () => {
    // TODO
  });
});
