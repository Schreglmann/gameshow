import { test, expect } from '@playwright/test';

// Spec: specs/config-generation.md
test.describe('Config generation (interactive)', () => {
  test.fixme('generate wizard produces a valid config.json', async () => {
    // Covered indirectly by npm run generate — here we could assert UI flow if exposed
  });
});
