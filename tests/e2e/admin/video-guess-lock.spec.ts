import { test, expect } from '@playwright/test';

// Spec: specs/video-guess-lock.md (Planned)
test.describe('Video-guess instance lock', () => {
  test.fixme('locked instance rejects PUT edits', async () => {
    // TODO
  });

  test.fixme('unlock-precheck lists missing caches and offline references', async () => {
    // TODO
  });

  test.fixme('locked instance plays from cache even when source file is offline', async () => {
    // TODO
  });
});
