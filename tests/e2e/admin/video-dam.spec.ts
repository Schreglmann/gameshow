import { test, expect } from '@playwright/test';

// Spec: specs/video-dam.md (Planned)
test.describe('Video DAM', () => {
  test.fixme('video tab lists files with thumbnails and probe badges', async () => {
    // TODO: once implemented, assert the listing UI
  });

  test.fixme('drag-drop a video uploads + auto-probes', async () => {
    // TODO
  });
});
