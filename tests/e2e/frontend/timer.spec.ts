import { test, expect } from '@playwright/test';

// Spec: specs/timer.md
test.describe('Timer', () => {
  test.fixme('timer counts down from configured value', async () => {
    // TODO: use a simple-quiz question with `timer: 10`, assert UI counts down
  });

  test.fixme('timer completion fires expected side effect', async () => {
    // TODO
  });
});
