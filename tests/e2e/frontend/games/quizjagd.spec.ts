import { test, expect } from '@playwright/test';

// Spec: specs/games/quizjagd.md
test.describe('Game type: quizjagd', () => {
  test.fixme('easy / medium / hard question tiers are selectable', async () => {
    // TODO
  });

  test.fixme('per-question points adjust the winning team in-place (can be negative)', async () => {
    // TODO
  });

  test.fixme('questionsPerTeam is respected', async () => {
    // TODO
  });
});
