import { test, expect } from '@playwright/test';

// Spec: specs/games/final-quiz.md
test.describe('Game type: final-quiz', () => {
  test.fixme('both teams place bets before the question', async () => {
    // TODO
  });

  test.fixme('answer reveal awards bet amount per team (positive or negative)', async () => {
    // TODO
  });
});
