import { test, expect } from '@playwright/test';

// Spec: specs/games/q1.md
test.describe('Game type: q1 (find the false statement)', () => {
  test.fixme('renders 3 true + 1 false statements in random order', async () => {
    // TODO
  });

  test.fixme('answer reveal highlights the wrong statement', async () => {
    // TODO
  });
});
