import { test, expect } from '@playwright/test';

// Spec: specs/games/ranking.md
test.describe('Game type: ranking (ordered-answer guess)', () => {
  test.fixme('answers reveal one at a time with correct rank prefixes', async () => {
    // TODO
    expect(true).toBe(true);
  });

  test.fixme('holding ArrowRight reveals all remaining answers', async () => {
    // TODO
  });
});
