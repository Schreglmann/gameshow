import { test, expect } from '@playwright/test';

// Spec: specs/games/bet-quiz.md
test.describe('Game type: bet-quiz (Einsatzquiz)', () => {
  test.fixme('team places a bet before question reveals', async () => {
    // TODO
  });

  test.fixme('correct answer awards +bet, wrong subtracts -bet', async () => {
    // TODO
  });

  test.fixme('category is shown to both teams before bet', async () => {
    // TODO
  });
});
