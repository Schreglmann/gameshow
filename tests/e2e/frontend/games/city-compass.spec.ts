import { test, expect } from '@playwright/test';

// Spec: specs/games/city-compass.md
test.describe('Game type: city-compass (Städte-Kompass)', () => {
  test.fixme('neighbors sit at their true bearing with the center hidden', async () => {
    // TODO
    expect(true).toBe(true);
  });

  test.fixme('the host advance reveals the center city', async () => {
    // TODO
  });

  test.fixme('progressive reveal adds one neighbor per advance', async () => {
    // TODO
  });
});
