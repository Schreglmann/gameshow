import { test, expect } from '@playwright/test';

// Spec: specs/games/fact-or-fake.md
test.describe('Game type: fact-or-fake', () => {
  test.fixme('statement is shown with FAKT / FAKE buttons', async () => {
    // TODO
  });

  test.fixme('answer reveal shows description', async () => {
    // TODO
  });
});
