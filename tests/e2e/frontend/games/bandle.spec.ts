import { test, expect } from '@playwright/test';

// Spec: specs/games/bandle.md
test.describe('Game type: bandle', () => {
  test.fixme('tracks play in sequence on phase advance', async () => {
    // TODO
  });

  test.fixme('hint is revealed only when hintEnabled', async () => {
    // TODO
  });

  test.fixme('releaseYear + difficulty are shown on answer', async () => {
    // TODO
  });
});
