import { test } from '@playwright/test';

// Spec: specs/local-transcode.md — covered inside frontend/games/video-guess.spec.ts
// where the preload wait + cached-segment playback is asserted end-to-end.
test.skip('local-transcode — covered inside video-guess.spec.ts preload assertions', () => {
  // no-op placeholder
});
