import { test } from '@playwright/test';

// Spec: specs/sync-bidirectional.md — requires NAS mount like nas-asset-mount.
test.skip('sync-bidirectional — requires NAS mount, covered by manual testing + sync-assets.ts unit tests', () => {
  // no-op placeholder
});
