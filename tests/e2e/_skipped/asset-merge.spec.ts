import { test } from '@playwright/test';

// Spec: specs/asset-merge.md — merge UX is covered in admin/admin-backend.spec.ts.
// This placeholder keeps the grep-for-coverage property (every spec has a
// sibling e2e file) without duplicating assertions.
test.skip('asset-merge — covered inside admin-backend.spec.ts "Merge" tests', () => {
  // no-op placeholder
});
